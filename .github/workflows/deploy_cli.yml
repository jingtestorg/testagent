name: Deploy to Joule Studio

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write # This is required for requesting the JWT
      contents: read  # This is required for actions/checkout
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Joule Studio CLI
        uses: SAP/lifecycle-operation-actions-for-joule-studio/actions/setup-jl@main

      - name: Fetch SCI token
        id: fetch-token
        uses: SAP/lifecycle-operation-actions-for-joule-studio/actions/token-fetch@main
        with:
          sciTenantUrl: ${{ vars.SCI_TENANT_URL }}
          sciClientId: ${{ vars.SCI_CLIENT_ID }}

      - name: Build solution
        id: build-solution
        uses: SAP/lifecycle-operation-actions-for-joule-studio/actions/build@main

      - name: Deploy solution
        uses: SAP/lifecycle-operation-actions-for-joule-studio/actions/deploy@main
        with:
          sciToken: ${{ steps.fetch-token.outputs.sciToken }}
          jouleStudioUrl: ${{ vars.JOULE_STUDIO_URL }}
          solutionZip: ${{ steps.build-solution.outputs.solutionZip }}
